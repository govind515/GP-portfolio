import EarthCanvas from "./Earth"; // Importing the EarthCanvas component from the Earth module.
import StarsCanvas from "./Stars"; // Importing the StarsCanvas component from the Stars module.

// Exporting the imported components for use in other parts of the application.
export { EarthCanvas, StarsCanvas };
